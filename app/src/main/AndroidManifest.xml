<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zust.playandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.PlayApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar.Animation"/>
        <activity
            android:name=".ui.register.RegisterActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.guide.GuideActivity" />
        <activity android:name=".ui.homepage.detial.ArticleDetialActivity"
            android:theme="@style/AppTheme.NoActionBar.Animation"/>
        <activity android:name=".ui.homepage.detial.BannerDetailActivity" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:theme="@style/AppTheme.NoActionBar.Animation" />
        <activity android:name=".ui.navi.detail.NaviDetailActivity" />
        <activity android:name=".ui.Project.list.detail.ProjectDetailActivity" />
        <activity android:name=".ui.search.detail.SearchDetailActivity" />
        <activity android:name=".ui.tree.Tree2Activity"></activity>
    </application>

</manifest>